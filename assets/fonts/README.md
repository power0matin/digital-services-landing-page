# Fonts

This project is configured to work with system fonts by default.

Recommended Persian font:

- Vazirmatn

To use local font files, download Vazirmatn from its official source and place the `.woff2` files in this directory, then add the related `@font-face` rules to `assets/css/styles.css`.

Font files are intentionally not bundled in this archive.
